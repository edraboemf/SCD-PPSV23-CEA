#################################################################################################
## README
## To execute this script, please place the module called "PPSV23_Model.R" in the main folder, and
## set rename the path of the main directory (line 25: ppsv23_model_dir <- "~/Users/") to the 
## correct path. Next, create a new directory called "libraries" in the same folder as the
## "PPSV23_Model.R" file and place in that ne directory all other modules. Place the parameters 
## file (Appendix B) in a subdirectory called "inputs". Run themodel by executing the module 
## "PPSV23_Model.R", after selecting the prefered settings.
#################################################################################################
